services:
  chat-client:
    build: .
    container_name: chat-client-prod
    ports:
      - "${FRONTEND_HOST_PORT}:5100"
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:5100/ || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 5